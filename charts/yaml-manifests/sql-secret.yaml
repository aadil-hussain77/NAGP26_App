apiVersion: v1
kind: Secret
metadata:
  name: sql-credentials
  namespace: nagp26
type: Opaque
stringData:
  DB_USER: "sa"
  DB_PASSWORD: "REPLACE_ME"
# In production, use sealed-secrets or External Secrets Operator to encrypt secrets.
